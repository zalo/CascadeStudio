# GENERATED OCP.TDF — proxies over the embind binding (gen-ocp-shim.mjs)
from ocp_registry import TDF_Label  # noqa: F401
class _Any:
    def __init__(self, *a, **k):
        raise NotImplementedError(
            'OCP.TDF.' + self.__class__.__name__ + ' is not in the generated shim')
class TDF_LabelSequence(_Any):
    pass
