# GENERATED OCP.TopTools — proxies over the embind binding (gen-ocp-shim.mjs)
from ocp_registry import TopTools_IndexedDataMapOfShapeListOfShape, TopTools_ListOfShape, TopTools_ShapeMapHasher  # noqa: F401
class _Any:
    def __init__(self, *a, **k):
        raise NotImplementedError(
            'OCP.TopTools.' + self.__class__.__name__ + ' is not in the generated shim')
class TopTools_HSequenceOfShape:
    def __init__(self, *a, **k):
        raise NotImplementedError(
            'PENDING_FORK_BINDING: OCP.TopTools.TopTools_HSequenceOfShape — Wire.combine (ConnectEdgesToWires); also retires COMPROMISE(edges-to-wires)')
class TopTools_IndexedMapOfShape:
    def __init__(self, *a, **k):
        raise NotImplementedError(
            'PENDING_FORK_BINDING: OCP.TopTools.TopTools_IndexedMapOfShape — topo_explore_connected_faces (TopExp.MapShapes)')
class TopTools_SequenceOfShape:
    def __init__(self, *a, **k):
        raise NotImplementedError(
            'PENDING_FORK_BINDING: OCP.TopTools.TopTools_SequenceOfShape — Shape.split bookkeeping')
