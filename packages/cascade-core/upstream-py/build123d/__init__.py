# build123d package (upstream-source mode). Registered EMPTY first so the
# seam submodules (geometry, topology) and the transformed upstream Level-A
# modules can be registered beneath it; build123d._finalize (registered
# last) populates this namespace with the user-facing API.
