# build123d.topology.shape_core — seam re-exports (objects_curve imports
# Shape/ShapeList from here; downcast is identity over lite's JS seam).
from build123d.topology import Shape, ShapeList, downcast  # noqa: F401
